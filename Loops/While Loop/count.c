// wap to count numbers using while loop

/*
#include <stdio.h>

int main()
{
    int i = 1, count = 0;
    while (i >= 10)
    {
        count = count + 1;
        count++;
    }
    printf("The count is %d", count);
    return 0;
}
*/

// Wap to count numbers using do while loop

#include <stdio.h>

int main()
{
    int i = 1, count = 0;
    do
    {
        count = count + 1;
        count++;
    } while (i >= 5);
    printf("The count is %d", count);
    return 0;
}

